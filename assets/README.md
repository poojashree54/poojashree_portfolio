# Assets Folder

Place your binary portfolio assets here:
- `profile.jpg`: Your professional profile headshot photograph.
- `resume.pdf`: If you choose to host your PDF locally instead of referencing the OneDrive URL, place it here as `resume.pdf` and link it inside `/index.html` as `./assets/resume.pdf`.
